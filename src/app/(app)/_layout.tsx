import { Stack } from 'expo-router';

// Placeholder layout for the authenticated app shell.
// Replace with Tabs when the feed, profile, and other screens are built.
export default function AppLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
