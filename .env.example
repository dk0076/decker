# Copy to .env and fill in your project values from the Supabase dashboard.
# These are the only two vars the app reads; never add the service_role key here.
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
